@model ShopRounding3rd.Domain.Entities.Product


<div class="well">
    <h4><strong>@Model.Name</strong></h4>
    <span class="lead">@Model.Description</span>
    <h5>
        <span class="pull-right label label-primary">@Model.Price.ToString("c")</span>
    </h5>
</div>