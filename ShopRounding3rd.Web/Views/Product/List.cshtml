@model IEnumerable<ShopRounding3rd.Domain.Entities.Product>

@{
    ViewBag.Title = "Products";
}

<h2>Products</h2>

@foreach (var item in Model)
{
    <div>
        <h3>@item.Name</h3>
        @item.Description
        <h4>@item.Price.ToString("c")</h4>
    </div>
}

